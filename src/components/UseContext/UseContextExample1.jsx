
//useContext is a react hook that allows you access data from any component without explicitly passing it down through props at every where
//usecontext is used to manage global data in the react app
//1.Creating the context 2. providing the context 

import React from 'react'

const UseContext = () => {
  return (
    <div>UseContext</div>
  )
}

export default UseContext